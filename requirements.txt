# CrewAI Multi-Agent System Dependencies
# Note: crewai[tools] includes all built-in tools like YouTube RAG tools
crewai[tools]>=0.80.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.2

# AI Model Dependencies - Google Gemini
langchain-google-genai>=2.0.10
google-generativeai>=0.8.0

# Embeddings for YouTube RAG Tools
langchain-mistralai>=0.2.0  # Mistral embeddings (prioritized)
# Google embeddings (fallback) already included in langchain-google-genai

# Vector Database for YouTube RAG Tools (IMPORTANT: full package, not chromadb-client)
chromadb>=0.4.22  # Required by CrewAI YouTube tools (use specific version to avoid conflicts)

# Core Dependencies
python-dotenv>=1.0.0
asyncio>=3.4.3

# Database
psycopg2-binary>=2.9.9

# Additional Tools (Optional - install as needed)
# google-search-results>=2.4.2  # For SerperDevTool
# tavily-python>=0.3.0  # For TavilySearchTool
# firecrawl-py>=0.0.5  # For FirecrawlTool
